== TODO ==

=== Next steps ===

 * Namespace support (in progress, Sebastian)
 * Node type support

For these functionalities we need to implement at least the following methods:

==== Level 1 ====
 * Node::hasNode(String relPath)
 * Node::hasProperties(String relPath)

==== Level 2 ====

Implementation may be basic, the inner working can always be refactored later.
If the methods need other objects and methods to be present, these have to be
implemented as well, of course.

=== Already done ===

==== Level 1 ====
 * Workspace::getNamespaceRegistry() (done, Karsten)
 * NameSpaceRegistry::* (done, Karsten)
 * Node::getPrimaryNodeType() (done, Karsten)

 * Node::getNodes() (done, Karsten)
 * Node::hasNodes() (done, Karsten)

 * Node::getProperties() (done, Karsten)
 * Node::hasProperties() (done, Karsten)
 

* Node::getNode(String relPath) (done, Sebastian)
* Node::getProperty(String relPath) ( done , Sebastian )

==== Level 2 ====
 * NameSpaceRegistry::* (done, Sebastian)
 * setProperty(String name, Value value) (done, Sebastian)
 * Node::addNode(String relPath) (done, Thomas & Sebastian)
 * Node addNode(String relPath, String primaryNodeTypeName) (done, Thomas & Sebastian)
 