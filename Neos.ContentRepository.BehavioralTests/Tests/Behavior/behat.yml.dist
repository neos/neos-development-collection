# Behat distribution configuration
#
# Override with behat.yml for local configuration.
#

default:
  autoload:
    '': "%paths.base%/Bootstrap"
  suites:
    cr:
      paths:
        - "%paths.base%/Features/"
      contexts:
        - FeatureContext:

