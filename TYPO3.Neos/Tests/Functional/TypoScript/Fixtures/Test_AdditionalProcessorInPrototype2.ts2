#
# Here, we test that the processor 1 is evaluated FIRST, and then processor 2
#

page1.body.sections.sidebar.prototype(TYPO3.TypoScript:TextWithHeadline).title << 1.wrap(prefix: 'BEFORE', suffix: 'AFTER')
prototype(TYPO3.TypoScript:TextWithHeadline).title << 2.wrap(prefix: '-b-', suffix: '-a-')