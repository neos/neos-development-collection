#
# This example modifies the rendering pf all texts inside the left column of a three column element, setting the headline to a static value
#

prototype(TYPO3.TypoScript:ThreeColumn).left.prototype(TYPO3.TypoScript:TextWithHeadline).headline = 'Static Headline'