#
# This example modifies the rendering pf all texts inside the three column element, setting the title to a static value
#

prototype(Acme.Demo:ThreeColumn).prototype(TYPO3.Neos.NodeTypes:Headline).title = 'Static Headline'
prototype(Acme.Demo:ThreeColumn).prototype(TYPO3.Neos.NodeTypes:Headline).title << 1.wrap(prefix: '<header>', suffix: '</header>')