#
# This example modifies the rendering inside the sidebar section, wrapping the title of the text
#

page1.body.sections.sidebar.prototype(TYPO3.TypoScript:TextWithHeadline).headline = 'Static Headline'