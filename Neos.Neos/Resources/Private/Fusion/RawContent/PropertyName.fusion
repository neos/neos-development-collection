prototype(Neos.Neos:RawContent.PropertyName) < prototype(Neos.Fusion:Component) {
    name = null

    renderer = Neos.Fusion:Component {
        nameParts = ${String.split(props.name, ':')}
        name = ${props.name}

        renderer = ${Translation.translate(props.nameParts[2],props.name,[],props.nameParts[1],props.nameParts[0])}
    }
}
